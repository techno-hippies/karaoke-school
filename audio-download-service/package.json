{
  "name": "slsk-service",
  "version": "1.0.0",
  "type": "module",
  "description": "Soulseek download service for karaoke pipeline",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run --watch index.ts",
    "postinstall": "node scripts/postinstall.cjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "latest",
    "slsk-client": "^1.1.0"
  },
  "devDependencies": {
    "patch-package": "^8.0.1"
  }
}
