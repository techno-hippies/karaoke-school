---
version: "2.2"

services:
  audio-download:
    image: t3333chn0000/audio-download-service:v2.5.0  # ✅ Fire-and-forget TikTok video uploads with DB updates
    expose:
      - port: 3001
        as: 80
        to:
          - global: true
        accept:
          - webdisplay
    env:
      - PORT=3001
      - NODE_ENV=production
      - DATABASE_URL=postgresql://neondb_owner:npg_OPrn6hIpM2dN@ep-snowy-sound-ag41lh1b-pooler.c-2.eu-central-1.aws.neon.tech/neondb?channel_binding=require&sslmode=require
      - NEON_PROJECT_ID=frosty-smoke-70266868
      - ACOUSTID_API_KEY=I9UjOdbcJK
      - SOULSEEK_ACCOUNT=bl4ckqat
      - SOULSEEK_PASSWORD=C3T$6z88@Fk4wN
      - CHAIN_ID=37111

profiles:
  compute:
    audio-download:
      resources:
        cpu:
          units: 2
        memory:
          size: 4Gi
        storage:
          - size: 30Gi
            # Storage for P2P downloads and temporary audio files

  placement:
    akash:
      pricing:
        audio-download:
          denom: uakt
          amount: 10000  # Max bid per block (~$0.01-0.03/hour)

deployment:
  audio-download:
    akash:
      profile: audio-download
      count: 1
