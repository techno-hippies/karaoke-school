{
  "name": "lit-sponsorship-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p api-bundled && esbuild api/mint-user-pkp.ts --bundle --platform=node --target=node18 --outfile=api-bundled/mint-user-pkp.js --format=cjs --external:@vercel/node",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@lit-protocol/auth": "^8.1.2",
    "@lit-protocol/lit-client": "^8.2.3",
    "@lit-protocol/networks": "^8.3.2",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/node": "^3.2.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vercel": "^37.0.0"
  }
}
