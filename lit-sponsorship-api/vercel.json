{
  "version": 2,
  "builds": [
    {
      "src": "api-bundled/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api-bundled/$1.js"
    }
  ]
}
