{
  "name": "audio-download-service",
  "version": "2.5.0",
  "type": "module",
  "description": "Multi-strategy audio/video download service (yt-dlp + Soulseek P2P) for karaoke pipeline",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run --watch index.ts",
    "postinstall": "node scripts/postinstall.cjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "latest",
    "slsk-client": "^1.1.0"
  },
  "devDependencies": {
    "patch-package": "^8.0.1"
  }
}
